const country = 'spain';
const population = '47 million';
const capitalCity = 'Madrid';

function countryInfo(country, population, capitalCity) {
    return (`${country} has ${population} and its capital city is ${capitalCity}`);
}

console.log(countryInfo(country, population, capitalCity));