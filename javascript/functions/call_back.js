function isString (str, callback) {
    if(str === string)
        console.log('The input that you just added is a string');    
}

function firstWordUpperCase (str, callback) {
    x = str.toUpperCase           
}